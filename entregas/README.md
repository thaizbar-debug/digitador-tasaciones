# entregas/

Aquí se descomprimen los paquetes que genera `web/index.html`. Cada trabajo vive en su propia subcarpeta:

```
entregas/
└── 2026-08-17-lopez-miraflores/
    ├── manifest.json
    ├── inputs/
    │   ├── fotos/
    │   ├── partidas-arancelarias/
    │   ├── pu/
    │   ├── borrador/
    │   └── otros/
    └── output/            (aquí guarda el skill el informe final)
```

## Importante

Esta carpeta está en `.gitignore`. Los trabajos reales contienen información sensible de clientes y **no se suben a GitHub**. Solo se conserva localmente.
